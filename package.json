{
  "name": "hcma2-teaching",
  "private": true,
  "type": "module",
  "description": "Static GitHub Pages frontend. This package.json exists only to run the Gate 1B.1 reader test suite (test/gate1b1) — it is not a build step for index.html, which stays a single deployable static file.",
  "scripts": {
    "test:pure": "node --test test/gate1b1/golden-legacy.test.mjs test/gate1b1/versioned-fixtures.test.mjs test/gate1b1/pii-canary.test.mjs",
    "test:emulator": "node test/gate1b1/run-emulator-tests.mjs",
    "test": "npm run test:pure && npm run test:emulator"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4"
  }
}
